package testCases;
/*http://www.testautomationguru.com/selenium-webdriver-automating-hoverable-multilevel-dropdowns
 * Actions: To perform hover action on an element using WebDriver, We would be using Actions API as shown below. 
 * Below script will the mouse to the middle of the element which is what we would like to achieve./*/


/*
package testCases;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;

public class mouseHover {
	WebDriver driver;

	/*Actions action = new Actions(driver);
	action.moveToElement(element or by).perform();*/
	
/*	Actions actions = new Actions(driver);
	WebElement mainMenu = driver.findElement(By.cssSelector("#navbar-collapse-1 > ul > li:nth-child(3) > a"));
	actions.moveToElement(mainMenu);
	
	WebElement subMenu1 = driver.findElement(By.cssSelector(""));
	actions.moveToElement(subMenu1);

	WebElement subMenu2 = driver.findElement(By.cssSelector("subLinklocator"));
	actions.moveToElement(subMenu2);
	actions.click().build().perform();
	
}
*/